# whatsnext reads one key, server-side only (Netlify functions), to argue your case.
# Named CLAUDE_API_KEY (not ANTHROPIC_API_KEY) to dodge Netlify's AI Gateway auto-injection.
CLAUDE_API_KEY=sk-ant-...
